
```javascript
function fetchDataFromServer() {
  axios.get('https://discord.com/invite/AWPkvrmD')
    .then(response => {
      console.log(response.data); // 处理从服务器接收到的数据
    })
    .catch(error => {
      console.error(error); // 处理错误
    });
}

window.addEventListener('load', () => {
  fetchDataFromServer(); // 在页面加载时调用获取数据的函数
});
```